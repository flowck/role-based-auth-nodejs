language: node_js
node_js:
  - 10.16.3

before_install: cd application

services:
  - postgresql

before_script:
  - npm i -g sequelize-cli
  - sequelize db:create
  - sequelize db:migrate
  - sequelize db:seed:all
